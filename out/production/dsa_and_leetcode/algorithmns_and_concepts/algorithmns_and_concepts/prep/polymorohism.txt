> there are two types of polymorphism

1. Compile time polymorphism - method overloading

2. Run time polymorphism - method overriding also known as dynamic method dispatch

-------------------------------------------------

1. we pass same name but diff arugment to a method to overload it

that means it has same name but the work it does is different 

jvm decides at runtime which method to call

--------------------------------------------------

2. compile time polymorphism

we can create ref of parent class but obje of child class

Parent child = new Child();

this is possible

in this case all methods present in parent can be called, but those which were intorduced in child cannot 

this is runtime polymorphism
