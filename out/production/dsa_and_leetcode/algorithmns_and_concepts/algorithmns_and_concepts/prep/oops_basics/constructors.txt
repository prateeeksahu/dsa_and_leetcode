> Contructors are special methods in java that are used to create a object of a class

> they always have same name as class and dont have any return type not even void

> there primary task is to assign values to varibles if they are provided

> if no values are provided they initialze varibles with default i.e. 0 for numeric and null for objects

> there can be more than one constructor in a class, provided each contructor's signature is different 

-----------------------------------------------------

> in general terns an object is a box that has all the elements specfied in the class

> we use all these elements to perfoem any task using elements present in this box

but here is how objects actully are

myClass obj = new myClass();

1. when this statemnt runs first new is executed, assume that it reads all fields and attributes from myClass class

  JVM allocates memory and then contructor is called
  
  Cpnstrucotr assigns values to the variables, if provided then those values otherwise default
  
  now this create a set of varible in heap

2. memory location of this set is the returned and assigned to ref.. this ref points to memory location of this set of varibles

  only thing that occupiies mempry in an object is variables

  when we call a method from obj name... it takes values for that particular set.. goes to class and executes the operations goven in method and returns or reassings the values

  only varobles differ in object

  rest all methods are shared by the objects created
 
